import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Profile() {

    return (
        <React.Fragment>
            <Typography variant="h1" noWrap sx={{ p: 2 }}>Mon profile</Typography>
        </React.Fragment>
    )
}