import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Social() {

    return (
        <React.Fragment>
            <Typography variant="h1" noWrap sx={{ p: 2 }}>Social</Typography>
        </React.Fragment>
    )
}